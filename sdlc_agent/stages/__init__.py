"""Six SDLC stages. Each module exposes a single `run()` function whose
input/output types are defined in `sdlc_agent.models`. This makes the pipeline
auditable and lets stages be re-platformed independently (Phase 2/3)."""
